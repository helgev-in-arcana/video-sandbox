[package]
name = "video-pipeline"
version = "0.1.0"
edition = "2024"
description = "実験用 動画ピクセルパイプライン（push / 1:1 / RGBA8）"

[dependencies]
rsmpeg = { version = "0.18", default-features = false, features = ["ffmpeg8"] }
anyhow = "1"
